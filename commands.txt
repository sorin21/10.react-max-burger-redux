1. npm install create-react-app -g
2. create-react-app my-app
3. cd my-app
3. npm start

4. npm install --save radium
5. npm run eject
  copy into webpack.config files
    modules: true,
    localIdentName: '[name]__[local]__[hash:base64:5]'

6. npm install --save prop-types

7. For routing to work
  npm install --save react-router-dom react-router